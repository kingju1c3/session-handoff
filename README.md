# session-handoff
Platform-agnostic protocol and skills for handing off a long-running AI agent session to a fresh one before context-window auto-compaction destroys fidelity. Includes ready-to-use adapters for Claude Code (local CLI terminal + Claude Code Remote/web/Desktop).
